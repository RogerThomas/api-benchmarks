"""Placeholder ROOT_URLCONF for Django Bolt — `manage.py runbolt` routes via
its own Rust router (autodiscovered from api.py), not Django's urlpatterns;
this only exists so Django's own URL machinery has something importable."""

urlpatterns = []

FROM rust:1-alpine AS build
RUN apk add --no-cache musl-dev
WORKDIR /src
COPY Cargo.toml ./
COPY src ./src
RUN cargo build --release

FROM alpine:3.20
COPY --from=build /src/target/release/upstream /bin/upstream
EXPOSE 8000
CMD ["/bin/upstream"]
